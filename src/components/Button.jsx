import React from "react";

const Button = ({ styles }) => (
  <button type="button" className={`bn62 py-4 px-6 font-poppins font-medium text-[18px] text-primary bg-green-gradient rounded-[12px] outline-none ${styles}`}>
    Get Started
  </button>
);

export default Button;
